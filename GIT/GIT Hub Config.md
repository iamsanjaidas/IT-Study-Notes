
Connect to git
git config --global user.name "YOUR_USERNAME"
git config --global user.email "YOUR_GITHUB_EMAIL"

To get the file
git clone https link

Before Working on the note : ALWAYS 
git pull

Then after doing work:

git add .
git commit -m " Update note"
git push